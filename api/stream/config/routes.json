{
  "routes": [
    {
      "method": "GET",
      "path": "/stream",
      "handler": "stream.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/stream",
      "handler": "stream.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/stream",
      "handler": "stream.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
